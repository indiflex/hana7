name: ttt workflow
on:
  push:
    branches:
      - develop
jobs:
  start_pwd:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        
      - name: echo
        run: echo "Start Ci/CD"
      - name: ls
        run: ls -al
